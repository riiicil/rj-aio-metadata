# Agent Log — 2026-09-10

---

<!-- NEW ENTRIES GO HERE — newest at top, oldest at bottom -->

## Session Entry 1 — Non-AI Release Switch, Strict Save Work Button Selector, and Deferred Adobe Stock Metadata Clearing

**Time:** ~10:30 WIB<br>
**Branch:** `task/platform-adapters`<br>
**Commit:** `fix(adobestock): non-ai release switch, strict save work button selector, and skip premature metadata clearing`<br>
**Product code changed:** YES<br>
**Pushed:** NO

### What Was Done
- **Skip Premature Metadata Clearing on Adobe Stock (`src/overlay/overlay.js`):**
  - Resolved user observation where metadata was being wiped at the beginning of card processing before Category, AI Declaration, or Language were selected.
  - Wrapped Step 5 in `overlay.js` with `if (this.platformId !== 'adobestock')`. On Adobe Stock, metadata clearing is handled surgically and sequentially inside `fillMetadata()` right before new title and keywords are injected.
- **Strict "Save work" Button Targeting (`src/adapters/AdobeStockAdapter.js`):**
  - Resolved critical issue where the automation clicked the green "Submit" moderation button after bulk save.
  - Root cause: `button.button--action` was used as a fallback selector, which also matched Adobe Stock's green moderation submit button (`button.button--action.green-600-background`).
  - Added `_findSaveWorkButton()` helper that queries `button[data-t="save-work"]` and inspects button text strictly matching "Save work" or "Save" while explicitly excluding any button containing `submit`, `kirim`, or `review` in its `data-t` or text content.
  - Added polling in `bulkSave()` to wait for Adobe Stock's React interface to enable the "Save work" button after "Select All".
- **Non-AI Release Switch ("Recognizable people or property?") Handling (`src/adapters/AdobeStockAdapter.js`):**
  - Added support for the standard non-AI workflow: when `isAiGenerated` is `false`, the adapter unchecks the Generative AI checkbox if checked and automatically sets the *"Recognizable people or property?"* switch to "No" (`input[data-t="has-release-no"]`, `input[name="hasReleases"][value="no"]`), with fallbacks to the surrounding switch label/container.
  - Synchronized non-AI release switch handling in both per-card `fillMetadata()` and post-automation `bulkSave()`.
- **Architectural Consultation on Centralized Logging (`LoggerService`):**
  - Evaluated user inquiry regarding centralizing console logging into a dedicated `LoggerService` or utility module.
  - Strongly affirmed recommendation with a detailed architectural breakdown covering DRY compliance, semantic log methods, configurable log levels, and UI extensibility.
- **Verification:**
  - `scratch/test_subphase_4_6.mjs`: 57/57 assertions passed.
  - `scratch/test_tier1_adapters.mjs`: 78/78 assertions passed.
  - `scratch/test_tier2_adapters.mjs`: 94/94 assertions passed.
  - `scratch/test_tier3_adapters.mjs`: 107/107 assertions passed.
  - Total assertions across test suites: 336/336 passed (0 failures).
  - 100% Zero Native Emoji policy strictly enforced.
