/**
 * RJ AIO Metadata Extension — Content Script Router
 * Detects the active microstock platform and mounts the floating overlay HUD.
 */

console.log('[RJ AIO Metadata] Content router loaded on:', window.location.hostname);
